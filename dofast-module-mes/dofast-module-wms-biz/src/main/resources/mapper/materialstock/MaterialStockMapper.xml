<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dofast.module.wms.dal.mysql.materialstock.MaterialStockMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="getOriginIdByBatchCode" resultType="java.lang.Long">
        SELECT id FROM (
                           (SELECT id, create_time FROM purchase_goods
                            WHERE batch_code = #{batchCode} AND deleted = 0
                            ORDER BY create_time DESC LIMIT 1)

                           UNION ALL

                           (SELECT id, create_time FROM pro_feedback
                            WHERE batch_code = #{batchCode} AND deleted = 0
                            ORDER BY create_time DESC LIMIT 1)

                           UNION ALL

                           (SELECT id, create_time FROM wms_rt_issue_line
                            WHERE batch_code = #{batchCode} AND deleted = 0
                            ORDER BY create_time DESC LIMIT 1)
                       ) AS combined_data
        ORDER BY create_time DESC
            LIMIT 1
    </select>

</mapper>
