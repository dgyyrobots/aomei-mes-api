<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dofast.module.wms.dal.mysql.issueline.IssueLineMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="getIssueLineListByFeedbackCodes" resultType="com.dofast.module.wms.dal.dataobject.issueline.IssueLineDO">
        SELECT * FROM wms_issue_line
        <where>
            <choose>
                <!-- 有报工单时 -->
                <when test="feedbackCodes != null and !feedbackCodes.isEmpty()">
                    <!-- 使用IN查询精确匹配 -->
                    feedback_code IN
                    <foreach collection="feedbackCodes" item="code" open="(" separator="," close=")">
                        #{code}
                    </foreach>

                    <!-- 使用FIND_IN_SET进行逗号分隔值匹配 -->
                    <foreach collection="feedbackCodes" item="code">
                        OR FIND_IN_SET(#{code}, feedback_code)
                    </foreach>
                </when>
                <!-- 无报工单时返回空结果 -->
                <otherwise>
                    1 = 0
                </otherwise>
            </choose>
        </where>
    </select>

</mapper>
