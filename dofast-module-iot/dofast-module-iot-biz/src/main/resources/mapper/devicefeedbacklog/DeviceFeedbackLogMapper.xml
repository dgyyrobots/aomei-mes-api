<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dofast.module.iot.dal.mysql.devicefeedbacklog.DeviceFeedbackLogMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <select id="getFinalDeviceFeedbackLog"  parameterType="java.lang.String" resultType="com.dofast.module.iot.dal.dataobject.devicefeedbacklog.DeviceFeedbackLogDO">
         select * from iot_device_feedback_log where device_code = #{deviceCode} and quantity > 0 order by id desc limit 1
    </select>

    <delete id="deleteByCreateTimeRange" parameterType="java.lang.String">
        <![CDATA[
            DELETE FROM iot_device_feedback_log
            WHERE  create_time >= #{startTime} AND create_time < #{endTime}  AND deleted = 0
        ]]>
    </delete>
</mapper>
